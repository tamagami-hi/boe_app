import { PendingScreen } from "~/features/shared/PendingScreen"

const Screen = (): React.ReactElement => (
  <PendingScreen title="Fund receipts" surface="fund receipt queue" />
)

export default Screen
