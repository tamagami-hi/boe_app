# ─────────────────────────────────────────────────────────────────────────────
# dev_release/.env.example — DEVELOPMENT stack configuration template.
#
#   cp .env.example .env      then fill in every value marked REQUIRED.
#
# ISOLATION FROM PRODUCTION (plan §7.3) — these must never be shared:
#   • database container and volume    (boe-dev-postgres / boe_dev_postgres_data)
#   • docker networks                  (boe_dev_frontend / boe_dev_internal)
#   • every secret and signing key     (this stack's .env only)
#   • cookie names, ports, container names
#
# The development stack must not contain production customer data.
#
# PORTS
#   All bound to 127.0.0.1; nginx is the only public entry point. Verified free
#   on the VPS. Postgres has NO host port.
# ─────────────────────────────────────────────────────────────────────────────

# ── release identity ────────────────────────────────────────────────────────
# BOE_VERSION is deliberately NOT here. The compose() wrapper in
# release_manager/stacks/_shared/_boe_lib.sh injects it as a process variable
# from BOE_VERSION_FOR_COMPOSE, which deploy.sh and rollback.sh set. A stale
# value in this file would be ignored by the tooling and would only mislead a
# manual `docker compose` run.

# ── runtime ─────────────────────────────────────────────────────────────────
# NODE_ENV stays "production" so the build behaves like the real thing; the
# environment is distinguished by hostnames, secrets and data, not by this flag.
NODE_ENV=development
LOG_LEVEL=debug

# ── host ports (127.0.0.1 only) ─────────────────────────────────────────────
BACKEND_PORT=47423
APP_FRONTEND_PORT=47421
ADMIN_FRONTEND_PORT=47422

# ── public origins ──────────────────────────────────────────────────────────
# The marketing site (beonedge.in, on AWS) is a standalone project and is NOT a
# browser origin of this application: it never loads this API from a page, it
# only makes a server-to-server POST to /newuser. So it is deliberately absent
# from CORS_ORIGIN — CORS governs browser fetches and would be meaningless here.
# WEB_ORIGIN_ALLOWLIST is authoritative at runtime (runtime/environment.ts:236);
# CORS_ORIGIN is only the legacy fallback used when the allowlist is unset, so
# when both are set the backend ignores CORS_ORIGIN entirely.
#
# It is REQUIRED anyway: the deploy gate lists it in boe_assert_env_keys
# (_boe_deploy.sh:213, which dies on an empty value) and boe_validate_app_policy
# (_boe_deploy.sh:276) additionally demands it start with https://. Leaving it
# blank fails the deploy before docker is touched. Keep it equal to the app origin.
PUBLIC_API_BASE_URL=https://dev-app.beonedge.in/api
CORS_ORIGIN=https://dev-app.beonedge.in
# `https://localhost` is the Android APK's own content origin, not a developer
# machine. Capacitor serves the bundle over androidScheme=https, so every request
# the client and admin APKs make carries `Origin: https://localhost`. CORS only
# reflects origins listed explicitly, so omitting it makes the entire APK look
# offline — every screen fails to load with no useful error. It must be the exact
# string: no port, no wildcard, and never `*`.
# Only add it to backends that actually serve an APK.
#
# The third entry is the BROWSER admin console. It is not a beonedge.in name:
# there is no public admin domain, and the console is served only on the tailnet
# MagicDNS name (see release_manager/nginx/admin.tailscale.conf). It must be
# listed because validateWebOrigin() allowlists the exact browser Origin and
# rejects everything else with 403 CSRF_INVALID — including a same-origin request
# whose Referer is not listed, which is what a missing entry actually looks like:
# the console loads, then cannot fetch its CSRF token.
#
# This replaces `https://dev-admin.beonedge.in`, which was listed here for months
# and never existed in DNS or in any nginx vhost.
#
# Every entry must be https. That is not cosmetic: WEB_COOKIE_SECURE=true below
# makes the session cookies __Host-boe_access/__Host-boe_refresh, and browsers
# only honour the __Host- prefix over HTTPS, so a cleartext admin origin cannot
# hold a session at all. The deploy gate enforces the same rule
# (_boe_deploy.sh:302) and dies on any non-https entry.
WEB_ORIGIN_ALLOWLIST=https://dev-app.beonedge.in,https://beonedge-vps.tail4ea2bc.ts.net,https://localhost
WEB_COOKIE_SECURE=true

SUPPORT_EMAIL=support@beonedge.in

# ── in-app APK updates ──────────────────────────────────────────────────────
# The release pipeline publishes each APK plus its sidecar JSON into these
# holder directories (release_manager/stacks/dev_release/paths.json). They are
# mounted read-only into the backend as /srv/boe/apk/<variant>, and nginx serves
# them publicly under APK_DOWNLOAD_BASE_URL — see the /downloads/ block in
# release_manager/nginx/dev-app.beonedge.in.conf.
#
# Blank APK_DOWNLOAD_BASE_URL means the update check still reports the newest
# build but hands out no download URL, which the app treats as "no update".
APK_CLIENT_DIR=/srv/dev_stack/BOE_APP/dev_release/dev_apk
APK_ADMIN_DIR=/srv/dev_stack/BOE_APP/dev_release/dev_admin_apk
APK_RELEASE_ROOT=/srv/boe/apk
APK_DOWNLOAD_BASE_URL=https://dev-app.beonedge.in/downloads

# ── database (separate from production) ─────────────────────────────────────
POSTGRES_DB=boe_app_dev
POSTGRES_USER=boe_app_dev
# URL-safe because Compose also interpolates this into DATABASE_URL.
# Generate with: openssl rand -hex 32
POSTGRES_PASSWORD=

# ── worker cadence (faster than production for quicker test feedback) ───────
PAYMENT_RECONCILIATION_INTERVAL_SECONDS=30
PAYMENT_RECONCILIATION_LEASE_SECONDS=60
PAYMENT_RECONCILIATION_MAX_BACKOFF_SECONDS=900
PAYMENT_RECONCILIATION_EXPIRY_GRACE_SECONDS=300
PAYMENT_RECONCILIATION_CLAIM_LIMIT=25
PHONEPE_PAYMENT_EVENT_ALLOWLIST=checkout.order.completed,checkout.order.failed
EMAIL_WORKER_INTERVAL_SECONDS=10
SIP_WORKER_INTERVAL_SECONDS=60
EMAIL_WORKER_CLAIM_LIMIT=25
EMAIL_WORKER_LEASE_MS=60000

# ── payments (never use live gateway credentials in development) ────────────
PAYMENT_PROVIDER=phonepe
PAYMENT_ATTEMPT_TTL_MS=900000
PHONEPE_MOBILE_SDK_ORDER_ENABLED=false
PHONEPE_AUTOPAY_ENABLED=false
PHONEPE_AUTOPAY_COLLECTION_ENABLED=false
COLLECTION_WORKER_INTERVAL_SECONDS=60
PHONEPE_API_TIMEOUT_MS=10000

# ── KYC policy (shorter TTLs to speed up testing) ──────────────────────────
KYC_CODE_TTL_MS=600000
KYC_CODE_MAX_ATTEMPTS=5
KYC_RESEND_COOLDOWN_MS=15000
KYC_VALIDITY_MS=31536000000
KYC_EMAIL_FROM=support@beonedge.in

# ── email transport ─────────────────────────────────────────────────────────
# REQUIRED for any mail to leave the stack. SMTP counts as configured only when
# HOST *and* USER *and* PASSWORD are all set (USER/PASSWORD are in the secrets
# section below). Leave any of them blank and the email worker keeps running,
# keeps exiting zero, and fails every delivery with
# EMAIL_TRANSPORT_NOT_CONFIGURED — retrying for ~42 hours before dead-lettering.
# Nobody gets a signup confirmation, and the only outward symptom is applicants
# saying they never received an email. The worker now warns once per pass when
# this is the case; check its logs first if mail is missing.
#
# Deployment policy requires Zoho implicit TLS on port 465.
# KYC_EMAIL_FROM must equal EMAIL_SMTP_USER — most providers reject a From that
# is not the authenticated mailbox.
EMAIL_SMTP_HOST=
EMAIL_SMTP_PORT=465
EMAIL_SMTP_SECURE=true

# ── admin bootstrap (development-only account) ──────────────────────────────
# seedAuth.ts derives the user id itself; there is no ADMIN_USER_ID.
ADMIN_LOGIN_ID=dev-admin@beonedge.in
ADMIN_FIRST_NAME=BeOnEdge
ADMIN_LAST_NAME=DevAdmin
ADMIN_PHONE=+910000000002

# ── seeding (safe to overwrite in development) ──────────────────────────────
SEED_AUTH_ENABLED=true
SEED_AUTH_ALLOW_PRODUCTION=true
SEED_AUTH_OVERWRITE=true
# Maximum devices one client may be signed in on at once. A login beyond this
# revokes the oldest device's session instead of being refused. SEED_CLIENT_EMAIL
# below is exempt from the cap (dev/QA account, many emulators at once).
#
# SEED_CLIENT_EMAIL is also the QA login the mobile app signs in with. seed:auth
# creates both accounts and this file is their only source of truth: EMAIL and
# PASSWORD must be set together (half-configured fails the seed rather than
# quietly skipping), and with SEED_AUTH_OVERWRITE=true the password below is
# reapplied on every deploy — so rotating it here really does rotate the login.
# The client gets no role grant and no application_id; PHONE/FIRST/LAST are only
# used when the row is first created, because phone_e164 is UNIQUE.
#
# ADMIN_PASSWORD and SEED_CLIENT_PASSWORD must both be 12-128 characters with no
# control characters. That is the same passwordInputSchema the login route applies
# to a submitted password, and seed:auth refuses anything shorter rather than
# storing a credential that could never be used to sign in: a short password
# hashes and stores happily, then fails every login at request validation before
# the hash is compared, which looks exactly like a wrong password.
MAX_NATIVE_DEVICES_PER_USER=3
SEED_CLIENT_EMAIL=
SEED_CLIENT_PHONE=+910000000003
SEED_CLIENT_FIRST_NAME=BeOnEdge
SEED_CLIENT_LAST_NAME=Client

# ─────────────────────────────────────────────────────────────────────────────
# SECRETS — fill these only in dev_release/.env on the VPS.
# Every value must differ from production. Generate the token/crypto set with:
#   cd backend_controller && npm run keys:generate
#
# That command emits exactly the keys below and nothing else. Access tokens are
# ES256 (SIGNING_KEY + VERIFICATION_KEYS + CURRENT_KID); refresh tokens are
# HMACed with REFRESH_HMAC_KEY. The old symmetric ACCESS_TOKEN_SECRET and
# REFRESH_TOKEN_SECRET were retired in BE-003 and are not listed here — nothing
# reads them, so setting them only creates the illusion of configured signing.
# ─────────────────────────────────────────────────────────────────────────────
ACCESS_TOKEN_ISSUER=
ACCESS_TOKEN_AUDIENCE=
ACCESS_TOKEN_CURRENT_KID=
ACCESS_TOKEN_SIGNING_KEY=
ACCESS_TOKEN_VERIFICATION_KEYS=
REFRESH_HMAC_KEY=
REFRESH_KEY_VERSION=
CSRF_KEY_VERSION=
CURSOR_HMAC_KEY=
CRYPTO_TOKEN_HASH_KEY=
CRYPTO_TOKEN_HASH_KEY_VERSION=
CRYPTO_CONSENT_IP_HMAC_KEY=
CRYPTO_CONSENT_IP_HMAC_KEY_VERSION=
CRYPTO_RECIPIENT_HMAC_KEY=
CRYPTO_RECIPIENT_HMAC_KEY_VERSION=
CRYPTO_RECIPIENT_ENC_KEY=
CRYPTO_RECIPIENT_ENC_KEY_VERSION=
CRYPTO_PAYMENT_TOKEN_ENC_KEY=
CRYPTO_PAYMENT_TOKEN_ENC_KEY_VERSION=
# The shared secret the AWS-hosted marketing site (beonedge.in) presents in the
# `x-signup-key` header on POST /newuser. It is the only thing that identifies
# that caller: the request is server-to-server, so there is no browser Origin to
# trust. Generate with:  openssl rand -hex 32
# If left blank the backend still boots, but /newuser refuses every caller.
NEWUSER_SHARED_SECRET=
ADMIN_PASSWORD=
SEED_CLIENT_PASSWORD=
# PhonePe is the only payment provider. All six PHONEPE_* values below are
# required together; a partial set refuses to boot in production and
# degrades to "unconfigured" (payment routes disabled) elsewhere.
PHONEPE_CLIENT_ID=
PHONEPE_CLIENT_SECRET=
PHONEPE_CLIENT_VERSION=1
PHONEPE_ENV=sandbox
PHONEPE_CALLBACK_USERNAME=
PHONEPE_CALLBACK_PASSWORD=
PHONEPE_REDIRECT_URL=https://dev-app.beonedge.in/payment-return
PHONEPE_CALLBACK_URL=https://dev-app.beonedge.in/api/v1/provider-events/phonepe/payment
PHONEPE_SUBSCRIPTION_CALLBACK_URL=https://dev-app.beonedge.in/api/v1/provider-events/phonepe/subscription
PHONEPE_SUBSCRIPTION_EVENT_ALLOWLIST=checkout.setup.order.completed,checkout.setup.order.failed,checkout.order.completed,checkout.order.failed,subscription.notification.completed,subscription.notification.failed,subscription.redemption.order.completed,subscription.redemption.order.failed,subscription.redemption.transaction.completed,subscription.redemption.transaction.failed
PHONEPE_CHECKOUT_ALLOWED_ORIGINS=https://mercury-uat.phonepe.com
PHONEPE_MERCHANT_ID=
EMAIL_SMTP_USER=
EMAIL_SMTP_PASSWORD=

# Shared read cache (optional). Unset means every read goes to PostgreSQL, which
# is correct but puts the entire browse load on the connection pool. In the
# release stacks this is provided by the `redis` service.
#
# REDIS_KEY_NAMESPACE must stay DISTINCT from the prod stack's (`boe-prod`), and
# must match this stack's compose default. It was `boe` in both templates, so
# generating a .env from either one collapsed both stacks onto a single
# namespace — the compose files already default to the correct per-stack values,
# which is the only reason this never bit.
REDIS_URL=redis://redis:6379
REDIS_KEY_NAMESPACE=boe-dev
# Cache ceiling for the redis service (compose: --maxmemory, allkeys-lru).
# Declared so the knob is discoverable; the compose default is the same value.
REDIS_MAXMEMORY=256mb
CACHE_APP_CONFIG_TTL_MS=30000
CACHE_CATALOG_TTL_MS=30000
CACHE_PUBLIC_CONTENT_TTL_MS=60000
